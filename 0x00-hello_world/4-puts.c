#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char Say[] = "\"Programming is like building a multilingual puzzle";
puts(Say); /*writes the string to stdout*/
return (0);
}
